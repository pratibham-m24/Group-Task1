- args:
    sql: CREATE TABLE "user"("user_id" serial NOT NULL, "user_name" text NOT NULL,
      "auth_token" text NOT NULL, PRIMARY KEY ("user_id") )
  type: run_sql
- args:
    name: user
  type: add_existing_table_or_view
