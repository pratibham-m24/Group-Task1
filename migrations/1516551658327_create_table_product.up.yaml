- args:
    sql: CREATE TABLE "product"("product_id" bigserial NOT NULL, "product_name" text
      NOT NULL, "product_category" text NOT NULL, PRIMARY KEY ("product_id") )
  type: run_sql
- args:
    name: product
  type: add_existing_table_or_view
